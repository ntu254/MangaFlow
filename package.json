{
  "name": "mangaflow",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "client",
    "mobile",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "npm run dev --workspace client",
    "dev:mobile": "npm run start --workspace mobile",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace client && npm run build --workspace server && npm run build --workspace mobile",
    "typecheck": "npm run typecheck --workspace client && npm run typecheck --workspace server && npm run typecheck --workspace mobile",
    "test": "npm run test --workspace server && npm run test --workspace client && npm run test --workspace mobile",
    "test:quick": "npm run typecheck && npm run test && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
